---
import Trip from '../../trips/[id].astro';
import type { Locale } from '../../../i18n/locales';
export const prerender = false;
const locale = Astro.currentLocale as Locale;
---
<Trip locale={locale} />
