---
import Rec from '../../recommendations/[id].astro';
import type { Locale } from '../../../i18n/locales';
export const prerender = false;
const locale = Astro.currentLocale as Locale;
---
<Rec locale={locale} />
